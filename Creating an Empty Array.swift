// Write your code below 🚇
//Now that we’ve initialized the scores array, we can add integers (and only integers) inside.

//Note: Arrays can only store items of the same data type. The type of the array cannot be changed after it’s declared.


var subwayAdult = [Int]()
print(subwayAdult)

var subwayChild = [Int]()
print(subwayChild)
